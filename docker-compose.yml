# Proje genel dizini için Docker Compose
version: '3.8'

services:
  # MySQL Veritabanı
  mysql-db:
    image: mysql:8.0
    container_name: otobus-db
    restart: always
    environment:
      MYSQL_DATABASE: otobus_db
      MYSQL_ROOT_PASSWORD: 14i18o21101L*
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql
    networks:
      - otobus-network

  # Spring Boot Backend
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: otobus-backend
    restart: always
    ports:
      - "8080:8080"
    environment:
      - SPRING_DATASOURCE_URL=jdbc:mysql://mysql-db:3306/otobus_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
      - SPRING_DATASOURCE_USERNAME=root
      - SPRING_DATASOURCE_PASSWORD=14i18o21101L*
      - JWT_SECRET=4261656C64756E67537072696E67426F6F7453656375726974794A5754546F6B656E
    depends_on:
      - mysql-db
    networks:
      - otobus-network

  # Nginx Frontend
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: otobus-frontend
    restart: always
    ports:
      - "80:80"
    depends_on:
      - backend
    networks:
      - otobus-network

networks:
  otobus-network:
    driver: bridge

volumes:
  db_data:
